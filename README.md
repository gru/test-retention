
# Kafka Retention Test Stand

Этот стенд предназначен для проверки поведения Apache Kafka при настройке **retention.bytes**, то есть автоматического удаления старых сегментов лога при превышении лимита размера топика — даже если consumer ещё не прочитал эти данные.

## 📁 Состав файлов

### docker-compose.yaml
Поднимает Kafka и Zookeeper. Ключевые параметры:

- `KAFKA_LOG_RETENTION_BYTES = 1048576` (1 МБ)
- `KAFKA_LOG_SEGMENT_BYTES = 262144` (256 КБ сегменты)
- `KAFKA_LOG_RETENTION_CHECK_INTERVAL_MS = 5000`
- Логирование удаления сегментов включено через:
  ```
  KAFKA_LOG4J_LOGGERS="kafka.log.LogCleaner:DEBUG,kafka.log.Log:DEBUG"
  KAFKA_LOG4J_ROOT_LOGLEVEL=INFO
  ```

### create-topic.sh
Создаёт тестовый топик test-ret-bytes.

### produce-messages.sh
Генерирует заданный объём данных и отправляет их в Kafka.

Использование:
```
./produce-messages.sh <MB> <topic>
```

### consume-messages.sh
Читает все сообщения из топика и выводит:
- количество сообщений
- общий объём в байтах
- объём в МБ

### grep-kafka-logs.sh
Фильтрует вывод `docker logs` по строкам:
```
./grep-kafka-logs.sh "Deleted"
```

### reset-kafka.sh
Очищает окружение (контейнеры + volumes).

---

## 🧪 Сценарий теста

### 1. Сбросить окружение
```
./reset-kafka.sh
docker compose up -d
```

### 2. Создать топик
```
./create-topic.sh
```

### 3. Записать 10 МБ данных
```
./produce-messages.sh 10 test-ret-bytes
```

### 4. Дождаться удаления сегментов
```
./grep-kafka-logs.sh "Deleted"
```

Ожидаемые строки:
```
Deleted log ... .log.deleted
Deleted offset index ...
Deleted snapshot ...
```

### 5. Прочитать данные из топика
```
./consume-messages.sh test-ret-bytes
```

Ожидаемый результат:
- Прочитано ~1 МБ данных
- Остальное Kafka удалила

---

## 🎯 Итог

Kafka удаляет данные **независимо от того, были ли они прочитаны**, если превышен `retention.bytes`.

